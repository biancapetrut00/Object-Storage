name: 'Running tests'
on:
  pull_request:
    types:
      - closed

jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: [self-hosted, linux]
    steps:
    - run: python3 -m testtools.run
      working-directory: workspace/Project/object_storage